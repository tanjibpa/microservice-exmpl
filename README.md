### Simple CSV file reader demo app

Just run `docker-compose up`, then `localhost:5000` in a browser. There's a file named `demo.csv` in the root directory, which you edit and upload in the app for testing.